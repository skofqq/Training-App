# Copy this file to keystore.properties and fill in your values.
# keystore.properties and the .keystore file are gitignored — keep them safe:
# every release update must be signed with the SAME key, or it cannot be
# installed over a previous version.
storeFile=release.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=training
keyPassword=YOUR_KEY_PASSWORD
